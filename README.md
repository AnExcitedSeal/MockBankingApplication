# MockBankingApplication
An application created in C# to mock the the application that is currently being used by banks globally.

Uses a SQLite database to locally store all user details.

Only a prototype.
